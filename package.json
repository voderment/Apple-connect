{
  "name": "apple-connect",
  "version": "0.1.0",
  "description": "CLI workflows for managing App Store Connect app metadata.",
  "type": "module",
  "bin": {
    "apple-connect": "./bin/apple-connect.js"
  },
  "scripts": {
    "lint": "node --check bin/apple-connect.js && find src test -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "jose": "^5.10.0",
    "yaml": "^2.7.0"
  }
}
